import { styled } from "styled-components";

const Cores = {
    black: "#000",
    white: "#fff"
}

export const Cabecalho = styled.header`
    display: flex;
    width: 100vw;
    align-items: center;
    justify-content: center;
`